<table>
	<tr>
		<th>User ID</th>
	    <th>User email</th>
	    <th>role</th>
	    <th></th>
	    <th></th>
		<th></th>
	  </tr>
	<% @users.each do |user| %>
	  <tr>	
	    <td><%= user.id %></td>
	    <td><%= user.email %></td>
	    <td><%= user.role %></td>
	    <td><%= link_to 'Edit', admin_edit_user_registration_path(user), :class => "admin_edit_user_registration_link fancybox.ajax" %></td>
	  </tr>
	<% end %>
</table>