$("form#new_post_type").bind('ajax:success', function(evt, data, status, xhr) {
	
		$.fancybox.close();
		// $('ul#hmenu').html("<%= escape_javascript(render :partial => 'layouts/menu_items') %>");
		
		
});