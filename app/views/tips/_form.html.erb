<%= simple_form_for(@tip, :validate => true) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :title, :label => false %>
    <%= f.input :description, :label => false %>
	<%= f.association :categories, :label => false, :as => :check_boxes %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
