<%= simple_form_for(@newsletter_subscriber) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :email %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
