<%= simple_form_for([:question, @answer]) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :content %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
