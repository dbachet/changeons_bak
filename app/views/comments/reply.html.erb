<p>
  <b>Title:</b>
  <%= @comment.title %>
</p>

<p>
  <b>Content:</b>
  <%= @comment.body %>
</p>

<p>
  <b>User:</b>
  <%= @comment.user_id %>
</p>

<p>
  <b>Post:</b>
  <%= @comment.post_id %>
</p>


<!-- New comment -->

<h1>New comment</h1>

<%= form_for(@reply, :url => create_reply_post_comment_path(@post, @comment), :method => :post) do |f| %>
  <% if @reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @reply.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

